import styled from 'styled-components';
import { timesBasePx } from '../../shared/mixins';

export const RestConsoleWrapper = styled.div`
  background-color: #ecf0f1;
  padding: ${timesBasePx(2)};
`;
