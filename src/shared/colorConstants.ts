const colors = {
  green: '#2ecc71',
  white: '#FFF',
  lightRed: '#ffdedb',
};

const buttonColors = {
  primary: colors.green,
  text: colors.white,
};

export const colorConstant = {
  button: buttonColors,
  colors,
};
